<section class="container">
  <h3 class="gallery-name">{{gallery.name}}</h3>
  <section class="flex-row justify-center">
    {{#each gallery.products as |product|}}
    {{> product-item}}
    {{/each}}
  </section>
</section>

<script src="/js/gallery.js"></script>